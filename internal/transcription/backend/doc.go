// Package backend provides private, local transcription execution and durable
// artifact storage. It owns neither queue scheduling nor application/runtime
// persistence and never publishes transcript content.
package backend
